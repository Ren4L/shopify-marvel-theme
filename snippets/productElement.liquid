<div data-id="{{ id }}" style="width: 300px; display:flex; flex-direction: column; align-items: center; font-size: 20px; border: 1px black solid; border-radius: 15px; padding: 15px; margin: 10px; ">
    <img src="{{image | image_url: width: 256 }}">
    <h2>{{ title }}</h2>
    <p>{{ description }}</p>
    <p><b>{{ price }}$</b></p>
</div>